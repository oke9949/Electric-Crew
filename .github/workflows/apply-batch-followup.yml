name: Apply batch upload review fixes

on:
  push:
    branches: ["chatgpt/batch-ai-document-upload"]
    paths: [".github/workflows/apply-batch-followup.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/batch-ai-document-upload
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-batch-followup.mjs
      - run: git diff --check
      - name: Commit fixes and remove temporary patch files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/BatchDocumentUpload.tsx
          git rm scripts/apply-batch-followup.mjs .github/workflows/apply-batch-followup.yml
          git commit -m "fix: harden batch upload classification UX"
          git push origin HEAD:chatgpt/batch-ai-document-upload
